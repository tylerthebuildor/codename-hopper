if (require.main === module) main();

export default async function main() {
  console.log(
    "Now i can change all the typing i want, i can write a whole story in here, it just depends. I havent even checked if the app works, just trying out git"
  );
}
